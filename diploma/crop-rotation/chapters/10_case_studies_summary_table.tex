\begin{table}[H]
\centering
\scriptsize
\caption{Содержательная сводка отдельных примеров}
\label{tab:case-studies-summary}
\setlength{\tabcolsep}{1.5pt}
\begin{tabular}{@{}>{\raggedright\arraybackslash}p{0.14\textwidth}
                >{\raggedright\arraybackslash}p{0.11\textwidth}
                >{\raggedright\arraybackslash}p{0.20\textwidth}
                >{\raggedright\arraybackslash}p{0.15\textwidth}
                >{\raggedright\arraybackslash}p{0.15\textwidth}
                >{\raggedright\arraybackslash}p{0.19\textwidth}@{}}
\toprule
Пример & Фактическая культура & Первые три кандидата модели & \makecell[l]{Кандидат с наибольшей\\графовой поддержкой} & Статус & Ключевой вывод \\
\midrule
Высокая уверенность
  & \texttt{forage\_hay}
  & \texttt{forage\_hay} ($p_1=0.930$), \texttt{corn}, \texttt{wheat}
  & \texttt{forage\_hay} ($gs=0.661$)
  & первая рекомендация верна; цель в первых трёх позициях; сильная согласованность
  & модель и граф знаний согласованы: фактическая культура совпадает с первой рекомендацией и имеет сильную графовую поддержку \\
\midrule

Средняя уверенность
  & \texttt{corn}
  & \texttt{corn} ($p_1=0.669$), \texttt{soybeans}, \texttt{forage\_hay}
  & \texttt{soybeans} ($gs=0.508$)
  & первая рекомендация верна; цель в первых трёх позициях; слабая согласованность
  & модель выбирает правильный \texttt{corn}, но граф знаний сильнее поддерживает \texttt{soybeans}; это пример конкурирующей
  альтернативы внутри ранжированного списка \\
\midrule

Низкая уверенность
  & \texttt{legumes}
  & \texttt{legumes} ($p_1=0.432$), \texttt{soybeans}, \texttt{fallow}
  & \texttt{soybeans} ($gs=0.568$)
  & первая рекомендация верна; цель в первых трёх позициях; слабая согласованность
  & первая рекомендация совпала с целевой культурой, но модельная уверенность и графовая поддержка для неё низкие; результат лучше
  читать как ранжированный список кандидатов, а не как жёсткое предписание \\
\midrule

Сложный класс
  & \texttt{soybeans}
  & \texttt{corn} ($p_1=0.689$), \texttt{cotton}, \texttt{soybeans}
  & \texttt{soybeans} ($gs=0.386$)
  & первая рекомендация неверна; цель в первых трёх позициях; слабая согласованность
  & фактическая культура не первая, но остаётся в ранжированном списке; граф знаний выделяет её как более семантически поддержанную
  альтернативу \\
\bottomrule
\end{tabular}

\vspace{0.4em}
\footnotesize $p_1$ --- вероятность первой рекомендации CatBoost; $gs$ --- \texttt{graph\_support\_score}.
\normalsize
\end{table}
